<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Products List</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 900px;
      margin: 20px auto;
      padding: 0 15px;
      background: #f9f9f9;
    }
    .product {
      border: 1px solid #ccc;
      padding: 15px;
      margin-bottom: 15px;
      background: white;
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    .product img {
      max-width: 150px;
      display: block;
      margin-bottom: 10px;
      border-radius: 5px;
    }
    .product h2 {
      margin: 0 0 10px;
    }
    .product a {
      color: #007bff;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <h1>Products List</h1>
  <% products.forEach(p => { %>
    <div class="product">
      <h2><%= p.product_name %></h2>
      <a href="<%= p.product_url %>" target="_blank">Product URL</a>
      <img src="<%= p.product_img %>" alt="<%= p.product_name %>">
      <p><strong>Brief Description:</strong> <%= p.product_brief_description %></p>
      <p><strong>Description:</strong> <%= p.product_description %></p>
      <p><strong>Starting Price:</strong> <%= p.starting_price %></p>
      <p><strong>Price Range:</strong> <%= p.price_range %></p>
      <p><strong>User:</strong> <%= p.user_name %></p>
      <a href="<%= p.product_link %>" target="_blank">More Info</a>
    </div>
  <% }); %>
</body>
</html>
